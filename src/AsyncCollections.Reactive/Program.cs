using AsyncCollections.Reactive;
using CollectionTesting;
using System.Collections.Concurrent;
using System.Diagnostics;
using System.Threading.Channels;

//await using var sub = new TimerSubject(500)
//    .Take(10)
//    .Select(x => (long)x.Elapsed.TotalMilliseconds)
//    .Subscribe(time => Console.WriteLine(time + " ms"));

//await sub.WaitAsync();

